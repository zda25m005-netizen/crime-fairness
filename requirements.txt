# core
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# models (GPU work). Install the CUDA build that matches your driver:
#   https://pytorch.org/get-started/locally/
torch>=2.0

# document generation (optional, for the slides and reports)
python-pptx>=0.6.23
reportlab>=4.0
